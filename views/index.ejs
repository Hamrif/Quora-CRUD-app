<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Quora</title>
</head>
<body>
    <h1 id="heading">Quora</h1>
    <% for(let post of posts){ %>
        <div class="post">
           
            <p id="userName">@<%=post.name %></p>
            <p><%=post.content %></p>
            <a href="http://localhost:3000/posts/<%=post.id%>"> <button class="btn"> View details</button></a>
            <a href="http://localhost:3000/posts/<%=post.id %>/edit"><button class="btn"> Edit </button></a>
            <form  id="deletebtn" method="post" action="/posts/<%= post.id %>?_method=delete">
            <button class="btn">Delete</button>
            </form>
        </div>
        <% } %>

        <br>
        <a href="http://localhost:3000/posts/new"> <button id="createbtn" class="btn"> Create post</button> </a>
</body>
</html>